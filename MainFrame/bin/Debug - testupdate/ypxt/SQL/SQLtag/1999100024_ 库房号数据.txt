select a.*,b.subsys_id,b.subsys_name,c.name unit_name
from yp_group_name a left join xt_subsys b on a.subsys_id=b.subsys_id 
     left join zd_unit_code c on a.dept_sn=c.code
where a.deleted_flag='0'
order by a.group_no
                        